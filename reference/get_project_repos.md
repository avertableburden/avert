# Get project repositories

get_project_repos() is a Get function that extracts data from an object.
Specifically, this function implements an algorithm to get project
repositories. The function returns Repositories (a character vector).

## Usage

``` r
get_project_repos()
```

## Value

Repositories (a character vector)
