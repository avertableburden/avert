# Get input data

get_input_data() is a Get function that extracts data from an object.
Specifically, this function implements an algorithm to get input data.
The function returns Parameters (an output object of multiple potential
types).

## Usage

``` r
get_input_data()
```

## Value

Parameters (an output object of multiple potential types)
